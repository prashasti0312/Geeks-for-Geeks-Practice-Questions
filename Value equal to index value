class Solution {
    ArrayList<Integer> valueEqualToIndex(int arr[], int n) {
         ArrayList<Integer> arrli = new ArrayList<Integer>(n);
        for(int i=0; i<n; i++)
        {
            if(arr[i]==i+1)
            arrli.add(i+1);
        }
        return arrli;
    }
}
